import React from 'react';

const SkillPill = ({ skill }) => (
  <span className="inline-block bg-gray-700 text-gray-300 text-sm font-medium mr-2 mb-2 px-3 py-1 rounded-full">{skill}</span>
);

export default SkillPill;
